{
  "name": "icns",
  "version": "0.1.0",
  "description": "Agent-first CLI to resolve Iconify icons and render PNGs",
  "type": "module",
  "bin": {
    "icns": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc --noEmit -p tsconfig.json",
    "dev": "bun run src/cli.ts"
  },
  "engines": {
    "bun": ">=1.1.0"
  },
  "dependencies": {
    "commander": "^14.0.1"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "typescript": "^5.9.2"
  }
}
